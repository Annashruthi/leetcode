def count_unique_countries():
    n = int(input())
    country_set = set()
    
    for _ in range(n):
        country = input().strip()
        country_set.add(country)
    
    print(len(country_set))

# Run the function
count_unique_countries()
